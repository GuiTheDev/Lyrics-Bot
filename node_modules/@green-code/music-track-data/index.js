/**
 * Simple JavaScript API to find lyrics of any song.
 * Unfortunately, not all songs have lyrics but it returns a comfortable to read
 * array of objects.
 * 
 * Supports: English songs (only these have been tested, I don't know about others).
 * Sources: deezer.com & lyrics.ovh
 * Author: github.com/beningreenjam
 */

 module.exports = require('./src/data');